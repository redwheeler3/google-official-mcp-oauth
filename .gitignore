# OAuth client secrets (Google's download format). Track the folder, ignore contents.
secrets/*
!secrets/.gitkeep

# Token caches contain long-lived OAuth refresh tokens.
.config/google-official-mcp-oauth/

# Local/editor noise.
node_modules/
.DS_Store
*.log
